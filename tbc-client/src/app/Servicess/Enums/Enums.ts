export enum AccountType
{
    Current      = 0,
    Saving       = 1,
    Accumulating = 2
}

export enum Currency
{
    GEL = 0,
    USD = 1,
    EUR = 2,
    RUB = 3,
}

export enum AccountState
{
    Active = 0,
    Closed = 1
}

export enum Gender
{
    Female = 0,
    Male   = 1
}